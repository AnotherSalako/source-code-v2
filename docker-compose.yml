services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: jupiter
      POSTGRES_PASSWORD: jupiter
      POSTGRES_DB: jupiter
    ports:
      - "5432:5432"
    volumes:
      - jupiter_pgdata:/var/lib/postgresql/data

volumes:
  jupiter_pgdata:
